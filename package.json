{
  "name": "glance",
  "version": "0.0.3",
  "private": true,
  "description": "Glance — fast, minimal Markdown reader and editor",
  "scripts": {
    "tauri": "tauri",
    "dev": "tauri dev",
    "build": "tauri build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  }
}
