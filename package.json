{
  "name": "simple-markdown-reader",
  "version": "0.0.2",
  "private": true,
  "description": "Fast, minimal Markdown reader and editor",
  "scripts": {
    "tauri": "tauri",
    "dev": "tauri dev",
    "build": "tauri build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  }
}
